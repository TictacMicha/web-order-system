apiVersion: v1
kind: Service
metadata:
  name: web-order-service
spec:
  type: NodePort
  sessionAffinity: ClientIP
  selector:
    app: web-order
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30080